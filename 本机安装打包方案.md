# 本机安装打包方案（YOLOv5-Lite）

> 适用仓库：`yolov5-lite-v5-5090`（v1.2.1）。v8/v11/v26 实施后复用同一方案。
> 模型权重已包含在 `app/src/main/assets/`（原始 v5lite 权重），无需额外下载即可构建。

---

## 一、Android 版（APK）本地构建与安装

### 1.1 前置依赖
- **Android Studio**（含 SDK Manager）
- **NDK**：SDK Manager → SDK Tools → NDK（Side by Side），建议 r25+（与 AGP 8.1.4 匹配）
- **CMake**：SDK Tools → CMake 3.22+
- **ncnn** 与 **opencv-mobile** 预编译包（Android）：
  - ncnn：https://github.com/Tencent/ncnn/releases （下载 `ncnn-xxxx-android-vulkan.zip`）
  - opencv-mobile：https://github.com/nihui/opencv-mobile （下载 `opencv-mobile-xxxx-android.zip`）
- JDK 17

### 1.2 修改本地依赖路径
打开 `app/src/main/jni/CMakeLists.txt`，把两处写死的作者机器路径改为本机实际解压路径：
```
set(ncnn_DIR "D:/Android-studio/ncnn-20210525-android-vulkan/$ANDROID_ABI")
set(OpenCV_DIR "D:/Android-studio/opencv-mobile-2.4.13.7-android/sdk/native/jni")
```

### 1.3 构建
```
git clone https://github.com/g101400/yolov5-lite-v5-5090
cd yolov5-lite-v5-5090
./gradlew assembleRelease
# 产物：app/build/outputs/apk/release/app-release.apk
```

### 1.4 安装到手机
```
adb install -r app/build/outputs/apk/release/app-release.apk
```
首次进入授予「相机」权限；Android 9 及以下还需授予「存储」权限（用于保存 CSV）。

### 1.5 常见问题
- **模型加载失败**：`assets/` 缺权重或文件名不符（应为 c/e/i8e/i8s/s 的 `.param`/`.bin`）。
- **「专为旧版 Android 打造」提示**：本版已通过 `targetSdk 34` 消除；若仍出现，确认 `app/build.gradle` 的 targetSdk 为 34。

---

## 二、Windows 版（EXE / 安装包）本地打包（路线 B：Python + PyQt5）

> 本版（v1.2.1）Android 已就绪；Windows 版按以下方案实施（复用同一版本号与 CSV 格式）。

### 2.1 环境
- Python 3.10+，虚拟环境（本工作区 `ai-lab/yolov5-lite-domain/.venv` 已含 torch/ultralytics）。
- 依赖：`pip install pyqt5 opencv-python onnxruntime`

### 2.2 导出 ONNX
```
# 在 yolov5-lite-domain 中用 ultralytics 导出
python export.py --weights weights/yolov5lite.pt --include onnx --imgsz 416
# 得到 yolov5lite.onnx
```

### 2.3 写 `win_app.py`
- PyQt5 窗口：顶部标题栏 + 状态栏 + 菜单「帮助/关于」+ 摄像头/图片画面；
- OpenCV `dnn` 或 onnxruntime 加载 `yolov5lite.onnx` 推理；
- 结果落盘到 `%USERPROFILE%/Downloads/YOLOv5Lite/detect_年-月-日.csv`（字段同安卓：时间、物体、置信度）。

### 2.4 打包 EXE
```
pyinstaller -F -w win_app.py
# 产物 dist/win_app.exe
```

### 2.5 打安装包（NSIS）
- 用本工作区现成 NSIS 脚本生成 `YOLOv5Lite-Setup-x.x.x.exe` 安装包；
- 安装后于开始菜单建立快捷方式，写入卸载信息。

---

## 三、多版本（v8 / v11 / v26）构建说明
- v8/v11 为 **anchor-free**，后处理（DFL / 端到端 NMS）与 v5 不同，**不能复用 v5 后处理**；
- 建议先抽 `ModelRuntime` 接口（见 `规划与方案.md` 第四节），换模型只新增一个 Runtime 实现；
- v26 暂预留通道，等官方权重与导出方式确定后再接入。

---

## 四、权重来源说明
本仓库 `assets/` 内权重 **直接取自原始上游** `ppogg/ncnn-android-v5lite` 的 `app/src/main/assets/`，未做任何替换或再训练，满足「权重用原来源码里边的」。如需重新拉取：
```
powershell -Command "Invoke-WebRequest -Uri https://raw.githubusercontent.com/ppogg/ncnn-android-v5lite/master/app/src/main/assets/e.param -OutFile app/src/main/assets/e.param"
# 依次下载 c / e / i8e / i8s / s 的 .param 与 .bin
```
