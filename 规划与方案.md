# YOLOv5-Lite 应用 · 实施记录与规划

> 项目路径：`D:/Users/Claw/ai-lab/yolov5-lite-app`（fork 自 `YOLOv5-Lite-v1.5/android_demo/ncnn-android-v5lite`）
> 当前版本：**v1.2.1**（versionCode 10201）

---

## 一、本轮已完成的代码改动（v1.2.1）

| 需求 | 实现 | 文件 |
|---|---|---|
| ① 安装后提示「专为旧版 Android 打造」 | `compileSdk/targetSdk` 由 **24 → 34**（该提示源于 targetSdk 过低） | `app/build.gradle` |
| ② 检测框标签英文 → 中文 | 新增中文类名表（80 类），native 侧 `YOLO_CN_LABELS` 开关；**状态栏/CSV 走 Java 映射，中文 100% 正常** | `jni/yolov5.cpp`、`Labels.java` |
| ③ 状态栏显示「几个物体、分别是什么」 | 顶部状态栏实时显示：`检测到 3 个物体：人 ×2、汽车 ×1` | `res/layout/main.xml`、`MainActivity.updateStatus()` |
| ④ 检测结果自动存储（时间/物体/置信度） | 写入**公共 `Downloads/YOLOv5Lite/`**，`detect_年-月-日.csv`，**按天一个文件保留历史**，带 BOM（Excel 中文不乱码） | `DetectionStore.java` |
| ⑤ 菜单「帮助」「关于」+ 版本号 | 右上角 ⋮ 弹出菜单；关于弹窗显示版本号 **1.2.1**（取自 Manifest，单一真相源） | `res/menu/main_menu.xml`、`MainActivity` |
| ⑥ 两个发布平台（安卓 / Win） | Android 已就绪；**Windows 方案见第三节**（需二选一实施） | 见下 |
| ⑦ 窗口与菜单美观优化 | Material 配色 + 顶部标题栏 + 状态栏着色 + 控制区重排 | `colors.xml`、`styles.xml`、`main.xml` |

### 关键设计说明

1. **中文为什么放在 Java 层映射？**
   OpenCV 的 `cv::putText` 使用 Hershey 字体，**不含中文字形**；若 OpenCV 未编译 freetype，画面框上的中文会显示成空白/方块。
   因此：状态栏与 CSV 的中文由 Java（`Labels.CN`）保证 100% 正确；画面框标签 native 侧已给中文，若出现方块，把 `yolov5.cpp` 的 `YOLO_CN_LABELS` 改为 `0` 即可回退英文。

2. **检测结果如何传到 Java？**
   新增 JNI 回传链路：`yolov5ncnn.cpp` 在每帧 `detect()` 后调用 `report_detections()`，
   经 `AttachCurrentThread` 回调 `Ncnnv5lite.onDetections("label:prob;...")`，
   再由 `Handler` 切回 UI 线程更新状态栏并落盘。写盘做了 **1.5s 节流**，避免刷屏写文件。

3. **Android 10+ 分区存储适配**
   API 29+ 用 `MediaStore.Downloads`（无需存储权限）；API ≤28 用公共目录直写（Manifest 已声明
   `WRITE_EXTERNAL_STORAGE` 且 `maxSdkVersion=28`）。

---

## 二、为什么这里不能编译 & 你在本地怎么构建

本机（沙箱）**无法**编译，三个硬阻塞：

1. **无 Android NDK**（只有 build-tools / platform-tools），ncnn 的 CMake 原生构建跑不了；
2. **无 Gradle**（且原工程 Gradle 5.4.1 与 JDK 17 不兼容、`jcenter()` 已停用）；
3. **缺 ncnn / opencv-mobile 依赖**：`jni/CMakeLists.txt` 里路径写死为
   `D:/Android-studio/opencv-mobile-2.4.13.7-android` 与 `D:/Android-studio/ncnn-20210525-android-vulkan`（原作者机器路径，本机不存在）。

### 我顺手修好的构建链（已改）
- `gradle-wrapper.properties`：Gradle 5.4.1 → **8.4**；
- 根 `build.gradle`：AGP 3.5.4 → **8.1.4**，仓库 `jcenter()` → `google() + mavenCentral()`；
- `app/build.gradle`：`compileSdk 34` / `targetSdk 34` / `namespace` / `versionCode 10201` / `versionName "1.2.1"` / AndroidX；
- `MainActivity`：`android.support.v4.*` → `androidx.core.*`（AGP 8 强制要求）。

### 你在本地构建的前置步骤
1. Android Studio 安装 **NDK** 与 **CMake**；
2. 下载 `opencv-mobile` 与 `ncnn-android-vulkan` 预编译包，把 `jni/CMakeLists.txt` 里两个 `set(..._DIR ...)` 改成你本机的实际路径；
3. 把模型文件放进 **`app/src/main/assets/`**（当前**缺失**）：每个模型需 `xxx.param` + `xxx.bin`，
   即 `320-lite-e / 416-lite-e / 320-lite-i8e / 416-lite-i8e / 416-lite-s / 416-lite-i8s / 512-lite-c`（与下拉框一致）；
4. 然后 `./gradlew assembleRelease` 出 APK。

> ⚠️ `assets/` 当前为空，不放模型的话 App 会提示「模型加载失败」。

---

## 三、Windows 版本方案（二选一，建议先做路线 B）

| | 路线 A：C++ / ncnn 原生 | 路线 B：Python + PyQt5 |
|---|---|---|
| 性能 | 高（原生 + Vulkan） | 中（OpenCV DNN / ONNX Runtime CPU，够用） |
| 工作量 | 大（需 ncnn Windows 库 + MSVC 配 OpenCV + 写 Win32/Qt GUI） | **小**（本工作区已有 PyQt5 + torch + 现成 NSIS 打包流水线） |
| 复用 | 与 Android 共用 `yolov5.cpp` 推理与后处理 | 需把后处理用 Python 重写一遍 |
| 推荐 | 长期目标 | **先出 Windows 版用它** |

**路线 B 落地步骤（建议顺序）**
1. `ai-lab/yolov5-lite-domain` 的 venv 里已有 torch/ultralytics；用 `export.py` 导出 `yolov5lite.onnx`；
2. 写 `win_app.py`：PyQt5 窗口（顶部标题栏 + 状态栏 + 菜单「帮助/关于」+ 摄像头画面），
   OpenCV `dnn` 或 onnxruntime 加载 ONNX 推理；
3. 结果落盘到 `%USERPROFILE%/Downloads/YOLOv5Lite/detect_年-月-日.csv`（字段同安卓）；
4. 用 PyInstaller 打 EXE，再用本工作区现成的 **NSIS** 脚本生成安装包。

---

## 四、多 YOLO 版本路线图（本项为规划，未实施）

### 版本号 → 模型映射（按你的定义）

| 版本区间 | 模型 | 状态 |
|---|---|---|
| **1.2.1 – 1.9.8** | YOLOv5（v5lite） | ✅ 当前 1.2.1 已落地 |
| **2.0.1 – 2.9.8** | YOLOv8 | 待实施 |
| **3.0.1 – 3.9.8** | YOLOv11 | 待实施 |
| **6.0.1 – 6.9.8** | YOLOv26 | **预留通道**（社区暂无官方 v26 权重，先占位，权重可用后再接入） |

版本号编码规则沿用：`versionCode = major*10000 + minor*100 + patch`
（1.2.1→10201，2.0.1→20001，3.0.1→30001，6.0.1→60001），保证单调递增、不回退。

### 架构改造：抽一层「模型运行时」

现状是 `yolov5.cpp` 把「加载 + 推理 + 后处理」写死在一处，换模型就要改 JNI 与 Java。
建议抽象为统一接口，让上层（Java / UI）只认结果：

```
ModelRuntime（C++ 接口）
 ├─ load(assets, modelId, backend)     // CPU / GPU
 ├─ detect(mat) -> vector<Object>      // 统一输出：label / prob / rect
 └─ 实现：Yolov5Runtime / Yolov8Runtime / Yolov11Runtime / Yolov26Runtime
```

**换模型只新增一个 Runtime 实现**，Java 层与 UI 零改动。

### 核心难点：后处理不通用

- **YOLOv5**：Anchor-based，输出 3 个尺度，需要 anchor 解码 + `box_score`/`unsig_pro`（现有 `yolov5.cpp` 已实现）；
- **YOLOv8 / v11**：**Anchor-free**，输出形状与解码方式完全不同（DFL / 端到端 NMS 差异），**不能复用 v5 的后处理**；
- **YOLOv26**：预留，等官方权重与导出方式确定后再定。

所以每换一代模型，主要工作量在**后处理实现 + ncnn 导出/量化**，而非 App 外壳。

### 分步实施建议

1. **第一步（现在）**：v1.2.1 打磨稳定 —— 中文、状态栏、存储、菜单、UI 已完成，先把 Android 版跑起来验收；
2. **第二步**：抽 `ModelRuntime` 接口，把现有 v5 逻辑迁进去（对外行为不变，纯重构）；
3. **第三步**：出 Windows 版（路线 B），与安卓共用同一套版本号与 CSV 格式；
4. **第四步**：接入 YOLOv8（2.0.1）——实现 anchor-free 后处理 + 导出 ncnn；
5. **第五步**：接入 YOLOv11（3.0.1）；v26（6.0.1）等权重可用后按同一接口接入。

---

## 五、当前阻塞项（需你侧提供）

1. **模型权重缺失**：`ai-lab/yolov5-lite-domain/weights/` 为空，App `assets/` 也为空。
   需要 v5lite 的 `.param`/`.bin`（安卓）或 `.pt`/`.onnx`（Windows）。
2. **沙箱无法访问 Google Drive**（此前记录：代理 502），若权重在 Google Drive，需转存后本地填写路径。
3. **安卓本地构建**需要你机器上的 NDK + ncnn/opencv-mobile（路径见第二节）。
